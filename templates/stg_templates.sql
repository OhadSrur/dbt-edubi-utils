{{
    edubi_utils.cte([('source','')],cte_ref_type='var')
}},

rename as (

    SELECT


    from source
),

final as(

    SELECT 
        -- Key
        
        -- Ref Key

        -- Attributes

        -- Flags

        -- Meta

    FROM rename
)

SELECT * FROM final