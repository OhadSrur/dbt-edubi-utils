{{
    config(
        primary_key="",
        post_hook=[
            "{{ edubi_utils.create_primary_key(this, '') }}"
        ]
    )
}}

{{
    edubi_utils.cte([('','')])
}},

final as (

    select
        -- Bridge Key

        -- Bridge Ref Key

        -- Key

        -- Ref Key
        
        -- Attributes
        
        -- Measures
        
        -- Flags
        
        -- Meta
        

    from 
)

select * from final